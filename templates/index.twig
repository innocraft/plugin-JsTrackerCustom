{% extends 'admin.twig' %}

{% set title %}{{ 'JsTrackerCustom_JsTrackerCustom'|translate }}{% endset %}

{% block content %}

    <div
        vue-entry="JsTrackerCustom.Admin"
        initial-custom-js="{{ customJs|default(null)|json_encode|e('html_attr') }}"
        custom-js-nonce="{{ customJsNonce|json_encode|e('html_attr') }}"
    ></div>

{% endblock %}
